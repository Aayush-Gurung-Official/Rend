# Backend Documentation

This folder is reserved for documenting the Node.js/Express backend of **Rend**.

- Architecture overview (folders like `auth`, `config`, `controllers`, `models`, `routes`, `upload`, `util`)
- API endpoint reference (e.g. `/api/listings`)
- Environment variables (see `backend/.env`)
- Docker and deployment notes

